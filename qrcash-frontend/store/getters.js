export const autenticado = state =>
  state.usuario.autenticado

export const getToken = state =>
  state.usuario.token

export const usuario = state =>
  state.usuario.usuario

export const cliente = state =>
  state.cliente
